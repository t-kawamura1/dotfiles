## 基本ルール

- 質問・調査・確認を求められた場合は、**必ず回答だけ**を返す
- ファイルの変更・作成・削除は、明示的に「修正して」「実装して」と指示された場合のみ行う
- 「〜なの？」「〜はどうなってる？」のような質問形式では、調査結果を報告するだけにとどめる
- 提案したい変更がある場合は、変更を加える前に「〜を修正してよいですか？」と確認を取る
- commit・push・PRの作成は、明示的に指示された場合のみ行う
- 番号付きリストは `1.` `2.` `3.` 形式を使う（①②③などの丸囲み数字はフォントによって文字化けするため使わない）
